/**
 * Public marketing / render site (Express `public/`).
 * Same-origin in production and in Vite dev (proxied — see vite.config.js),
 * so dashboard links never hard-jump to :3100 and a stale /app build.
 */
export const SITE_HOME = '/';

export const COMPANY_PHONE = '(941) 239-7919';
export const COMPANY_PHONE_TEL = '+19412397919';
export const COMPANY_EMAIL = 'Corp@FLPpros.com';
